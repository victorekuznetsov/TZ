# WK CRM — план работ

Единый портал по экскаваторам WK: каталог запчастей с чертежами, база знаний,
контроль запасов, обеспеченность, закупки и управление ремонтами.

Ветка разработки — `catalog-crm`. Позже репозиторий переводится в приватный.

## Статус

Этапы 1–3, 5–7 из раздела 6 реализованы и закоммичены (`build/*.py` + рабочий
портал `index.html`/`app.js`/`styles.css`, проверено Playwright без ошибок
консоли на всех вкладках, светлой/тёмной теме и мобильной раскладке).
Актуальные цифры по прайсу, запасам, обеспеченности и парку — в разделе 2,
они пересчитаны сборщиками, а не оценены вручную. Раздел 3 (чертежи)
обновлён по факту двух реальных экспериментов (реестр по имени файла и
пилот OCR), а не по одной лишь оценке трёх маршрутов.

---

## 1. Источники

### TZ · `main` — прайсы

| Файл | Строк | Содержимое |
| --- | --- | --- |
| Прайс-лист ДП … Мэйлинь | 3 042 | артикул, артикул обновлённый, наименование RU + ZH, класс 备件分类, модель, тип запчасти, ресурс м/ч, ТНВЭД, пошлина, **цена CNY DDP** |
| Прайс-лист УСО ПМ СЛ | 3 055 | тот же каталог в разрезе УСО, 9 колонок |

Пересечение 3 031 артикул, **227 позиций с разной ценой** (УСО дешевле),
24 только в УСО, 11 только в ДП. Сумма ДП — 192,7 млн CNY.
WK-35 — 1 487 позиций, WK-20 — 898, WK-20&WK-35 — 437, WK-20C — 204.
Ресурс проставлен у всех: 2 500 м/ч — 899, 5 000 — 1 791, 10 000 — 85.

**Юани живут только в каталоге.** В запасах, обеспеченности, ремонтах и
закупках всё в рублях — так, как их считает SAP.

### TZ · `rawdata` — 1 273 файла, 3,47 ГБ

**`rawdata/LinkOme`** — 8 электронных каталогов (по числу бортов/серий в
листе «Тех парк») в формате **LinkOne** (тот самый формат, в котором
Komatsu поставляет свои EPC), собраны в 6 книг:

| Книга | Борта | Листов |
| --- | --- | --- |
| K1623 | WK-20 №2, №3 | 148 |
| K1626 | WK-20 №1, №2, №8, №1220 | 156 (3 архива, изначально 2 неполные — дозалиты пользователем) |
| K1637 | WK-20C №3–6 | 166 |
| K1641 | WK-20C №1221 | 168 |
| K1839 | WK-35 №4–7 | 167 |
| K1849 | WK-35 №10–12, №14–16 | 67 |

Все архивы читаются и распаковываются как zip. Разобраны заголовки файлов
(`.bli` — состав листа, `.ilg` — чертёж, `parts.pix` — индекс номеров,
`book.bbi`/`book.cat` — оглавление): смещения полей размера, геометрия и
палитра иллюстраций. Тело сжато **собственным энтропийным кодером**
(7,79 бит/байт); zlib, lzma, bzip2, LZW и PKWARE DCL проверены и
исключены; публичного конвертера нет — сам чертёж из готовых архивов
пока не извлекается. **Но имена файлов внутри архива читаются без
декодирования** (`build/build_linkome_index.py`) — это даёт точную
проекцию: 872 листа, 633 уникальных номера, из них 397 из 2299 узлов
дерева (17,3%) и 208 из 3042 позиций прайса (6,8%) — как раз то, что
станет доступно, если маршрут A или B когда-нибудь заработает. См. раздел 3.

**`rawdata/АТ майнинг`** — документация, 1 255 файлов, 3,2 ГБ:

| Класс | Файлов | МБ |
| --- | ---: | ---: |
| Руководства по ремонту | 99 | 1 310 |
| Руководства по эксплуатации | 90 | 603 |
| Чертежи CAD (dwg/dxf) | 506 | 250 |
| Электросхемы и электрооборудование | 47 | 133 |
| Взаимозаменяемость и комплекты | 9 | 74 |
| Монтаж и сборка | 21 | 46 |
| Кондиционирование | 22 | 33 |
| ТО, нормы и регламенты | 29 | 25 |
| Смазка и ГСМ | 7 | 14 |
| Противопожарная система | 10 | 12 |
| Паспорта машин | 19 | 6 |
| Прочее (пневмо, барабан, тормоза, безопасность, фото) | 80 | 245 |
| *Каталоги в PDF* | *316* | *458* |

По моделям: WK-35 — 1 196 файлов, WK-20C — 31, WK-20 — 19, общие — 9.

**Главная находка — `Взаимозаменяемость узлов и деталей экскаваторов WK4.7.xlsx`.**
2 573 строки, три листа (WK-20, WK-20C, WK-35), восемь комплектаций:
K1623, K1626, K1637, K1641, K1839, K1849, K1853, K1866.
В каждой строке: механизм, каталожный номер узла, наименование на китайском
и русском, **количество на машину**, номер и название механизма — и признак
взаимозаменяемости между комплектациями (854 «Да» + 266 «Идентичны»).
Итого **2 299 каталожных номеров, 212 узлов, 52 механизма** — это готовая
структура каталога и спецификация, не требующая распознавания.

Рядом: `Каталожные номера на узлы в сборе.xlsx`, `Карты ППР WK 20 с ЕТО.xlsx`,
`Разбивка комплекта футеровки WK-20, WK-35.xlsx`, `Кабельный журнал WK-35.xls`,
ведомости и перечни документации.

### for_update — выгрузки SAP, перезалиты, читаются все три

| Файл | Строк | Содержимое |
| --- | --- | --- |
| `Остатки_14_09_2026.xlsx` | 193 347 | BW MM-M03: остаток по позиции, складу, БЕ; номер детали производителя, изготовитель. Всего 329,0 млрд ₽ |
| `Закупка ALL_Развитие_11_09_2026 v3.XLSX` | 25 341 | 42 колонки: заявка, дата заявки, закупщик, ЕКМТР, изготовитель, цена, валюта, количество, заказ, поставщик, **ещё поставить**, в пути, плановая и фактическая даты поставки. Всего 16,8 млрд ₽ |
| `Запас с ограниченным использованием 14_09_2026.XLSX` | 1 641 | 1 381 материал; запас ограниченного использования 32 728 ед. / 383,8 млн ₽ + блокированный 340 ед. / 21,3 млн ₽ |

### TOPO — дашборд ТОиР, 19 вкладок

Витрины `cube.json` (2,76 млн строк PM-06), `mtr.json` (133 090 материалов
с каталожным номером, ППЗ и изготовителем + 23 558 групп аналогов КСУ),
`ktg.json` (1 585 единиц, КТГ/КИО помесячно), `data/<площадка>_<год>.json`
(детализация «заказ → единица → материал»). Дизайн-система «Развитие»
и браузерный конвейер пересборки витрин из xlsx (`lib/`) переиспользуются.

### CAT — каталог Caterpillar

19 машин, 14 744 схемы, 94 477 номеров, 263 291 фото. Механика каталога
(дерево → схема → чертёж рядом с таблицей позиций → карточка детали → поиск →
проверка списка → корзина) — образец для каталога WK. Данных по WK там нет.

---

## 2. Что показали данные

### Парк

**28 экскаваторов** Taiyuan Heavy Industry (исправлено `build_fleet.py`:
в ktg.json 31 запись, но 3 — дубли карточек одной физической единицы,
WK-35 №06, WK-20 №1220, WK-35 №1224; схлопнуты): WK-35 — 16, WK-20 — 7,
WK-20C — 5. КТГ 0,771–0,892. 24 из 28 связаны с книгой комплектации по
листу «Тех парк» — он же даёт прямую связку книга ↔ серийник ↔ борт ↔
площадка, включая K1853/K1866 (закрывает прежний открытый вопрос 4).

### Ремонты 2022–2027

56 493 строки по 28 единицам, 2 896 материалов,
план **14 887 млн ₽**, факт **9 101 млн ₽**.
Виды работ (факт): текущий ремонт 2 674, капитальный 2 251, «не присвоено»
1 363, регулярно-потребляемые МТР 857, ТО-1 535, ТО-2 305.
Лидеры: WK-35 №04 — 1 450 млн ₽, WK-35 №06 — 915, WK-35 №05 — 707.

### Кодификация: половина прайса не заведена в НСИ

ППЗ **3.1.2.7 «Запчасти к экскаваторам WK» — 1 333 кода ЕКМТР**
(изготовитель TZ — 1 252, Siemens — 18, Nordfelt — 16, Мэйлинь — 3).

| | Позиций | Доля |
| --- | ---: | ---: |
| Прайс сопоставлен с ЕКМТР | 1 279 | 42 % |
| — неоднозначно (>1 кода) | 103 | |
| **Не кодифицировано** | **1 763** | **58 %** |
| Стоимость некодифицированного | 96,8 млн CNY | 50 % прайса |
| Привязано к дереву узлов (`tree.json`) | 660 | 21,7 % |

Цифры — из `catalog.json` (сборка `build_catalog.py`), не ручная оценка.

### Запасы на 14.09.2026

Остаток есть у **997 кодов WK из 1 333 (75 %)**: 33 705 ед. на **3 515 млн ₽**.

> Эта цифра исправляет прошлую оценку «50 кодов, 9,7 млн ₽»: та была снята
> со служебного словаря витрины TOPO, а не со свежей выгрузки остатков.

### Запас с ограниченным использованием — отдельный статус

Позиции, которые **лежат на складе, но в ремонт взять нельзя**.
По WK: **17 кодов, 40 единиц, 139,7 млн ₽ — 4 % стоимости запаса WK**,
и это самые дорогие узлы: ковши K1839.01.00 и K1623.01.00, редукторы
поворота K1605.07.02.00 и K1803.07.02.00, редукторы хода K1803.16.01.00,
рукояти K1601.02.00 и K1801.02.00SH, стрела K1801.03.15.00, дно ковша
K1839.01.03.00, корпус коробки K1801.05.04.00.

**У 7 кодов доступного остатка не остаётся вовсе** — весь их запас ограничен.
Самый дорогой случай: стрела K1801.03.15.00 — потребность 51,8 млн ₽,
на складе 2 штуки, обе ограничены.

Правило для отчёта: ограниченный и блокированный запас **вычитается**
из доступного и **подсвечивается отдельным статусом** везде, где считается
обеспеченность, точка заказа и вовлечение. Позиция, у которой весь остаток
ограничен, показывается как дефицит, а не как наличие.

### Обеспеченность плана 2026–2027

3 426 заказов без единого факта, 16 151 строка, потребность **7 003 млн ₽**.
Номенклатура WK внутри неё — 878 позиций на **4 784 млн ₽**:

| Обеспеченность доступным остатком | Позиций | Потребность |
| --- | ---: | ---: |
| полностью | 465 | 965 млн ₽ (20 %) |
| частично | 234 | 3 419 млн ₽ (71 %) |
| нет вовсе | 179 | 400 млн ₽ (8 %) |

Крупнейшее необеспеченное: цепь гусеничной ленты K1812.18.04.00 — 61 млн ₽,
стрела K1801.03.15.00 — 52 (остаток ограничен), вал напорный
K1812.03.21.00 — 41, круг роликовый K1812.13.00 — 21.

### Закупки

833 строки по номенклатуре WK на **1 716 млн ₽**, 460 кодов из 1 333.
**830 строк ещё не закрыты** (1 715 млн ₽), «ещё поставить» 6 575 единиц,
в пути 89. Плановые даты поставки: 2026 — 323 строки, 2027 — 483, 2029 — 4.
Поставщики: ДИЦИОН (406), Мэйлинь (245), Сибтрейдсервис (6).
639 строк в юанях — прайс CNY связывается с закупкой напрямую.
Крупнейшее: ковш K1839.01.00 — 323,6 млн ₽, канат K1801.05.10.00 — 79,7.

### Взаимозаменяемость

Групп взаимозаменяемости КСУ, куда входит хоть одна позиция WK, — 67 из 23 558.
В ведомости АТ-Майнинг — **189 строк** с явной отметкой «Да»/«Идентичны»
(из 944 строк с ≥2 разными номерами по трём листам WK-20/WK-20C/WK-35);
`build_tree.py` схлопнул их union-find'ом в **138 групп, 345 уникальных
номеров** (`interchange.json`) — это уже дедуплицированный материал для
заведения групп в КСУ, а не сырые отметки. Ещё 755 строк без явной
отметки (119 — с примечанием о частичной замене) — на ручной разбор,
не объединены автоматически.

---

## 3. Чертежи каталога: что проверено на практике

Структура каталога (узлы, состав, количества) закрывается ведомостью
взаимозаменяемости — она разобрана, реализована в `build_tree.py` и
вопросов не вызывает. Открытый вопрос был по **самим чертежам и
позиционным номерам на них**. Ниже — результаты практических экспериментов.

**LinkOme (формат LinkOne / ImageLink) — РАЗОБРАН.** Пользователь навёл на
соседнюю песочницу `KOMATSU_PARTS_BOOK`: там тот же формат (LinkOne под
другим брендом продавца ПО — Komatsu, Schramm, Cat, Cummins и WK/Тайюань
используют один контейнер) уже полностью разобран и читает 26 515 страниц
и 14 884 чертежа Komatsu. Декодер (`container.py` — заголовок и цепочка
блоков LZH «-lh5-», LZSS 8 КБ + динамический Хаффман, алгоритм LHA;
`book.py` — дерево разделов и строки позиций; `lzh.py` — сама
распаковка) портирован в `build/linkone/` этого репозитория один в один,
с одним отличием: у книг WK кодовая страница 936 (GBK — китайские
иероглифы), а не 932 (японская) — добавлена в `book.py`. Кириллица в тех
же строках (русский перевод вперемешку с китайским без разделителя)
декодируется тем же `gbk`, что и китайский.

Проверено на всех 7 уникальных книгах WK (K1623, K1626, K1637, K1641,
K1839, K1849, K1861 — K1626 роздана по трём архивам на разные борта с
одинаковым содержимым, у K1839 внутри архива задвоена папка книги, у
K1861 сама книга лежит во вложенном zip внутри присланного архива
(`unpack_linkome.py` разворачивает и такие), дубли отброшены по сигнатуре
модель+число_страниц): **1281 страница открылась, 19 760 строк позиций**
с номером детали, наименованием (кит./рус.) и количеством — это
**настоящий состав узла**, не проекция по именам файлов. Сопоставление с
деревом и каталогом:

| | Покрытие |
| --- | --- |
| Узлы дерева, где номер страницы = номер узла | 678 / 2276 (29,8%) |
| Узлы дерева, где номер детали встретился в строке любой страницы | **1957 / 2276 (86,0%)** |
| Позиции каталога, где номер детали встретился в строке любой страницы | **2293 / 3003 (76,4%)** |

Это на порядок выше прежней оценки «17,3%/6,8% — проекция по именам
файлов, если формат когда-нибудь декодируется»: формат декодирован,
проекция сбылась и сильно перевыполнена (реальные номера деталей внутри
строк совпадают с деревом/каталогом чаще, чем номера страниц с номерами
узлов). `build/build_linkome_catalog.py` — сборка, `data/linkome_catalog.json`
— результат, раздел карточки детали «Состав в LinkOme» — где это видно
в портале.

**K1861 — новая книга, борт не подтверждён.** По заголовку книги
(俄罗斯Q4矿区T008024905号WK-35挖掘机, заводской № 08024905) — WK-35 на
Сухом Логе. В «Тех парк» (`fleet_books.json`) её нет — лист не обновлялся
с момента поступления книги. На площадке 1200 (Сухой Лог) в `fleet.json`
есть ровно три борта WK-35 без книги и без заводского номера — №1222,
№1223, №1224 (КТГ появляется только в последних 1–3 месяцах, машины
совсем новые). По словам пользователя, книга относится к 3-му и 4-му
вводимым на площадке экскаваторам WK-35 — вероятно, закрывает один или
оба борта из №1223/№1224. Точная связка заводской № → борт нужна только
для карточки в разделе «Парк», на рост покрытия состава узла (сопоставление
идёт по номеру детали, не по борту) не влияет — см. `quality.json`.

**Что осталось нерешённым — растровый чертёж (.ilg).** Текстовая часть
(BBI/BLI) декодирована полностью, растровая — нет. У книг Komatsu
чертёж — сырой поток CCITT Group 4 сразу за 78-байтным заголовком, без
LZH (в отличие от текста, изображение уже почти не сжимается дальше).
У книг WK LZH-цепочка для .ilg есть, но начинается на нефиксированном
смещении (варьируется внутри одной книги — от файла к файлу), а
распакованный поток не собирается в валидный G4 ни при одной из
проверенных комбинаций начального сдвига внутри распакованных данных,
порядка бит (MSB/LSB-first) и фотометрии (BlackIsZero/WhiteIsZero).
Нужен либо более глубокий разбор именно этой части контейнера (нет
эталона для сверки, в отличие от текста, который сошёлся с реальными
номерами деталей), либо маршрут A (LinkOne Viewer, Windows/Wine — не
запускается в этой песочнице).

**Реестр по имени файла (без OCR).** У WK-35 часть чертежей — уже
предразбитые постраничные PDF/DWG, где номер детали — начало имени файла
(«2. Каталог/Мех», «2. Каталог/Элек», «Каталог(новый)», «Чертежи деталей
WK-35» — 683 файла). `build_drawings.py` сопоставил их со словарём
(объединение `tree.json` и `catalog.json`): **647 файлов с номером, 166
уникальных, из них 51 подтверждён словарём — 43 узла дерева (1,9%),
48 позиций прайса (1,6%)**. Низкое покрытие ожидаемо: эти файлы в
основном по общей книге K1801 (узлы, разделяемые между K1839/1849/1853/
1866), а не по книгам, которыми размечен сам прайс. Это уже РЕАЛЬНЫЕ
файлы (не проекция) — портал ссылается на них напрямую.

**Пилот OCR на каталоге-скане.** `tesseract` установлен и работает,
`pymupdf` рендерит страницы каталогов-сканов WK-20 (233 с.) и WK-20C
(176 с.) в чёткие изображения — маршрут технически рабочий. Но пилот на
30 случайных страницах WK-20 (рендер + OCR штампа + сверка со словарём)
дал точное совпадение только на 10 из 30: страницы содержат **таблицу
позиций с несколькими номерами**, а не один номер в углу листа, поэтому
надёжное распознавание требует разбора раскладки листа (какой candidate —
номер сборки, какие — позиции таблицы), а не просто OCR угла. Не
масштабировано на все 409 страниц вслепую — это отдельная, более
крупная задача с открытым результатом.

**Вывод.** Маршрут B (разбор формата) для текстовой части — пройден,
итог выше блока «LinkOme»: 86,0%/76,4% настоящего состава узла, в
портале уже сейчас. Для самого растра приоритет — снова маршрут A
(LinkOne Viewer, Windows/Wine), пока нерешённая раскладка растровых
записей у поставщика WK не поддастся отдельно. Реестр по имени файла и
рендер каталогов-сканов остаются в портале как рабочий, хоть и намного
более узкий (1,9–6,8% против 86,0–76,4%), но пока единственный источник
непосредственно картинки.

Правило пользователя учтено: чертежи берём из LinkOme; каталоги в PDF в
базу знаний **не** кладём — только там, где LinkOme книги нет
(`build_kb.py` исключает их явно). По картам «Тех парк» книг нет для
WK-20 №09 и WK-35 №1222/1223/1224 (4 борта из 28 не связаны ни с одной
комплектацией — см. `quality.json`).

---

## 4. Архитектура портала

Песочница **TZ** становится единым порталом. Статический сайт, без бэкенда,
всё считается в браузере — как TOPO и CAT.

```
TZ/  (ветка catalog-crm)
├── index.html              портал: оболочка, навигация, общие фильтры
├── styles.css              дизайн-система «Развитие»
├── app/
│   ├── catalog.js          каталог и карточка детали
│   ├── kb.js               база знаний
│   ├── fleet.js            парк, КТГ, ремонты
│   ├── crm.js              обеспеченность, запасы, закупки, кодификация
│   └── core.js             витрины, фильтры, таблицы, графики, выгрузки
├── data/
│   ├── catalog.json        3 042 позиции прайса × 2 299 номеров ведомости
│   ├── tree.json           52 механизма → 212 узлов → детали, количества
│   ├── inter.json          189 отмеченных строк → 138 групп взаимозаменяемости
│   ├── fleet.json          28 единиц: паспорт, комплектация, КТГ/КИО
│   ├── repairs.json        свод ремонтов; repairs_<год>.json — детализация
│   ├── stock.json          остатки + ограниченный запас + закупки
│   ├── kb.json             реестр документов: класс, модель, узел, ссылка
│   └── media/<узел>/       чертежи SVG и растры
├── lib/                    xlsx_stream.js, zip_write.js (из TOPO)
├── build/                  сборщики витрин (python)
└── rawdata/                ветка rawdata — сырьё, в портал не попадает
```

### Ключ связывания

```
ведомость взаимозаменяемости ──► дерево: механизм → узел → деталь, кол-во
        │                                   │
        │                                   ├──► чертёж (LinkOme / PDF / DWG)
        │                                   └──► документы (АТ-Майнинг)
        ▼
  каталожный номер ──┬─ прайс ДП и УСО (цена CNY, ресурс, ТНВЭД)
                     └─ «Номер детали производителя» в mtr.json
                              ▼
                        код ЕКМТР ──┬─ остатки (минус ограниченный запас)
                                    ├─ закупки (ещё поставить, в пути, сроки)
                                    ├─ расход в заказах ТОиР
                                    └─ аналоги КСУ
                                          ▼
                             единица техники ── КТГ / КИО / наработка
```

Сопоставление прайса с НСИ даёт 42 % автоматически; остальное — раздел
«Кодификация» и файл правил `build/aliases.csv`, переживающий пересборку.

### Единый портал

Одна оболочка, один набор фильтров (площадка, модель, борт, узел, год,
вид работ), сквозной поиск по номеру и наименованию, общая корзина-заявка.
Разделы TOPO (ТОиР по всему парку) и CAT (каталог Caterpillar)
подключаются перекрёстными ссылками по коду ЕКМТР: клик по номеру в WK CRM
открывает его же в витрине TOPO, и наоборот. Переносить TOPO и CAT внутрь
TZ не нужно — они работают и живут своей жизнью.

**Реализовано** (карточка детали каталога): ссылка «Открыть в TOPO по коду
ЕКМТР» — `https://victorekuznetsov.github.io/TOPO/?ekmtr=<код>#mtr`. Со
стороны TOPO (ветка `claude/spare-parts-repair-report-qnnmx4` там же)
`?ekmtr=` переключает на вкладку «МТР / ЕКМТР» и подставляет код в её
собственный поиск — логика поиска не менялась, только инициализация.
Обратной ссылки TOPO → WK CRM нет: TOPO задеплоен (GitHub Pages), а вот
WK CRM ещё нигде — вести ссылку некуда до решения по хостингу (открытый
вопрос 2). Добавится одной строкой, когда портал где-то появится. CAT не
связываем — это парк Caterpillar без кодов ЕКМТР, сопоставлять нечего.

---

## 5. Разделы портала

| # | Раздел | Содержание |
| --- | --- | --- |
| 1 | **Сводка** | Парк, КТГ, затраты, запас, **ограниченный запас**, обеспеченность, закупки, кодификация |
| 2 | **Каталог** | Дерево 52 механизма → 212 узлов → деталь. Чертёж рядом с таблицей позиций, позиция на чертеже = строка таблицы. Количество на машину, цена CNY, ресурс, ТНВЭД, код ЕКМТР, остаток, статус ограничения. Фильтры по модели и комплектации, поиск по номеру без учёта дефисов и по обоим наименованиям |
| 3 | **Карточка детали** | Прайс ДП и УСО рядом, взаимозаменяемость между комплектациями, применяемость по бортам, история расхода, остаток по складам с пометкой ограничения, открытые закупки со сроками, аналоги КСУ, ссылки на документы |
| 4 | **База знаний** | 936 документов АТ-Майнинг (без каталогов) по классам и узлам: ремонт, эксплуатация, ТО и нормы, электросхемы, смазка, пневмо, пожарная система, кондиционер, барабан, тормоза, паспорта. Привязка «узел → документ», полнотекстовый поиск по извлечённому тексту |
| 5 | **Парк** | 28 единиц: комплектация (K16xx/K18xx), КТГ/КИО помесячно, наработка, затраты, ₽/маш-час, сравнение бортов одной модели |
| 6 | **Ремонты** | План-факт 2022–2027 по единицам, узлам, видам работ; сезонность; средний чек; отклонение факта от плана |
| 7 | **Обеспеченность** | 3 426 открытых заказов: покрытие **доступным** остатком с MRP/ATP-распределением, светофор, дефицитный лист. Ограниченный запас — отдельной строкой «есть, но нельзя» |
| 8 | **Запасы** | 997 позиций с остатком, ограниченный и блокированный запас, неликвиды, страховой запас «ресурс × парк × срок поставки», точка заказа |
| 9 | **Закупки** | 833 строки на 1 716 млн ₽: воронка заявка → заказ → в пути → поставлено, просроченные сроки, покрытие дефицита, план против прайса, разрез по поставщикам |
| 10 | **Кодификация** | 1 761 позиция прайса без кода ЕКМТР — рабочий список на заведение НСИ с ценой, узлом и приоритетом по потребности; 107 неоднозначных связок; 106 кодов ППЗ без цены |
| 11 | **Взаимозаменяемость** | 189 отмеченных строк → 138 групп из ведомости против 67 групп в КСУ — предложения на заведение групп |
| 12 | **Цены** | Сверка ДП и УСО (227 расхождений), стоимость парка в запчастях, курс CNY→₽ |
| 13 | **Качество данных** | Что не сошлось: дубли единиц в КТГ, строки без кода, неполные книги LinkOme, неоднозначные связки |
| 14 | **Методика** | Источники, даты актуальности, формулы, границы применимости |
| 15 | **Обновление данных** | Загрузка новых выгрузок в браузере, пересборка витрин, скачивание json |

CRM-часть — разделы 7–11: дефицитный лист, точка заказа, состояние закупок,
список на кодификацию и на заведение аналогов. С ними работают, а не смотрят.

---

## 6. Этапы

| Этап | Содержание | Статус |
| --- | --- | --- |
| **1. Каркас данных** | `build_tree.py` (дерево + fleet_books.json из «Тех парк»), `build_catalog.py`, `build_fleet.py` | ✅ `tree.json`, `catalog.json`, `fleet.json` |
| **2. Связка с SAP** | Матчинг артикул ↔ ЕКМТР (`ekmtr_match.py`) + через узел-родителя | ✅ ЕКМТР 42 % (1279/3042); дерево 26,3 % (800/3042, было 21,7 %) |
| **3. Запасы, закупки, ремонты** | `build_stock.py` (остатки **минус ограниченный запас**), `build_repairs.py`, `build_provision.py` | ✅ `stock.json`, `repairs.json`, `provision.json` |
| **4. Чертежи / состав узлов** | `build_drawings.py` (реестр по имени файла), `build_linkome_catalog.py` (формат LinkOne разобран — реальный состав узла), пилот OCR | ✅ состав: 86,0% дерева, 76,4% каталога (7 книг, включая новую K1861). ◐ растровый чертёж (.ilg) — не декодирован, см. раздел 3 |
| **5. Оболочка портала** | `index.html`, дизайн-система «Развитие», навигация, поиск, карточка детали, курс CNY→₽, CSV из любой таблицы | ✅ 14 разделов, проверено Playwright |
| **6. Каталог и карточка** | Раздел 2–3: поиск, фильтры, join с деревом/ЕКМТР/остатком/чертежами, drill-down | ✅ |
| **7. База знаний** | `build_kb.py` (реестр 936 документов) + `build_kb_text.py` (полнотекстовый поиск) | ✅ реестр + поиск по 319/375 кандидатов (PDF/DOCX/PPTX и легаси .doc/.ppt — свой разбор формата, без LibreOffice) |
| **8. Парк и ремонты** | Разделы «Парк» (график КТГ по моделям + спарклайны), «Ремонты» | ✅ |
| **9. CRM** | Обеспеченность, Запасы, Закупки, Кодификация, Взаимозаменяемость, **Заявка** (корзина из трёх разделов, CSV, печать) | ✅ рабочие, на свежих данных |
| **10. Сводка, методика** | Разделы «Сводка», «Методика», «Качество данных» | ✅ |
| **11. Обновление данных** | Браузерный конвейер пересборки `stock.json` (остатки+ограничения+закупки) | ✅ `lib/xlsx_stream.js` + `lib/stock_pipeline.js`, сходится с офлайн-сборкой до рубля. Прайсы/дерево/парк/ремонты — по-прежнему только через `python3 build/*.py` |
| **12. Хостинг** | GitHub Pages из ветки `catalog-crm` (`.github/workflows/pages.yml`) | ✅ репозиторий приватный, деплой настроен; плюс полностью автономный режим — скачать ветку и открыть `index.html` без сервера |

---

## 7. Открытые вопросы

Сняты с прошлой версии: **вопрос 1** (курс CNY→₽) — ручной ввод в
каталоге реализован; **вопрос 2** (недостающие тома LinkOme) —
пользователь дозалил, оба архива читаются; **вопрос 4** (K1853/K1866) —
лист «Тех парк» дал прямую связку книга ↔ борт, K1853 = №17/№18,
K1866 = №13 (борт №18 и №13 делят один заводской номер 08023901B —
похоже на замену/капремонт узла, стоит уточнить у АТ-Майнинг, но для
портала не блокер); **формат LinkOne** (был вопрос 1 в прошлой версии) —
декодирован (раздел 3), текстовая часть работает в портале; остался
только растровый чертёж — см. пункт 1 ниже.

Актуальные:

1. **Растровый чертёж LinkOme (.ilg) не декодирован — второй, отдельный
   заход тоже не дал результата.** Контейнер и текстовая часть (состав
   узла) читаются, картинка — нет. Второй заход продвинул структурный
   разбор (цепочка LZH-блоков у растра бимодальна по смещению внутри
   одной книги — 142/1102 байт в K1637, найдено дешёвой проверкой
   структуры без распаковки), но сам кодек не идентифицирован: и CCITT
   Group 4, и Group 3 (1D/2D) через libtiff дают правдоподобную на вид
   долю чёрных пикселей, но при визуальной проверке — нарастающий шум,
   не чертёж (признак потери синхронизации декодера, то есть неверного
   кодека). RLE, другой словарь LZH и векторная раскладка записей тоже
   исключены. Подробности — `data/quality.json`. Без эталона для сверки
   (в отличие от Komatsu, где были book.rpt/parts.rpt) дальнейший перебор
   вслепую — не гарантирующая результата работа, сопоставимая по объёму
   с изначальной оценкой маршрута B (3–10 дней). Резервный путь —
   маршрут A (LinkOne Viewer, Windows/Wine — вне этой песочницы).
~~2. Хостинг~~ — закрыто. Репозиторий переведён в приватный (решение
   пользователя), задеплоен на GitHub Pages из ветки `catalog-crm`
   (публикуются только файлы портала — не прайсы, не `build/`); плюс
   полностью автономный режим — скачать ветку и открыть `index.html`
   без сервера, данные грузятся через `<script src>`, а не `fetch()`.
3. **4 борта без комплектации** — WK-20 №09, WK-35 №1222/1223/1224 — не
   упомянуты в листе «Тех парк» вовсе (не проблема архивов, лист их
   просто не перечисляет). Нужно уточнить у АТ-Майнинг, какой книге они
   соответствуют. Книга K1861 (новая, раздел 3) по словам пользователя —
   для 3-го и 4-го вводимых на площадке экскаваторов WK-35, то есть,
   вероятно, накроет один-два из №1223/№1224 — но прямой связки
   заводской № → борт по-прежнему нет, «Тех парк» не обновлялся.
4. ~~104 документа .doc/.ppt не индексируются для полнотекстового
   поиска~~ — закрыто. LibreOffice headless по-прежнему не работает в
   этой песочнице, но вместо него `build_kb_text.py` разбирает бинарный
   формат сам (OLE2 через `olefile`): .doc — FIB → таблица кусков
   (Clx/PlcPcd) → текст (CP1252/UTF-16LE), .ppt — плоский проход по
   TextCharsAtom/TextBytesAtom. Из 104 кандидатов проиндексировано 103
   (1 .doc — почти пустой файл-заглушка, реального текста в нём нет).
