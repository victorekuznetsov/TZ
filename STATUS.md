# WK CRM — статус ветки `grok`

Единый снимок на 21.09.2026. Предыдущие аудиты GBT не отменяются, но цифры
и открытые ограничения сверяются отсюда.

База: ветка `GBT` (`00b7c68`). Эта ветка — соседняя: те же витрины, другой UI и навигация.

## Что исправлено в grok

1. Дублирующийся `id="loBody"` в каталоге LinkOne (дерево и полноэкранный чертёж).
2. Деплой Pages не копировал `schedule.js` / `schedule.css` — план-факт на сайте был бы пустым.
3. Стартовая загрузка тащила LinkOne и базу знаний сразу; теперь ядро, остальное — по вкладке.
4. Корзина на каждую кнопку перечитывала `localStorage`; теперь кэш в памяти.
5. Хэш хранил только вкладку. Теперь `#provision&site=2400&order=…&part=…&role=supply`.
6. Глобальный поиск больше не сбрасывает на «Базу знаний»: палитра (Ctrl+K).
7. 14 вкладок без ролей. Добавлены рабочие места: снабжение / ТОиР / справочник.
8. Остаток не подписывал площадку. В запасах — склады с эвристикой площадки и «сколько здесь / из общего».
9. Заявке не хватало площадки, машины и заказа. Они пишутся в строку и в CSV.
10. На сводке пакет «заказать сегодня» (верхние 15 позиций `inTime`) кладётся в заявку разом.
11. Кодификация сортируется по приоритету (цена × есть ли узел в ведомости), не только по ¥.
12. В обеспеченности видны прямые замены ведомости — без пересчёта ATP и без транзитива.
13. `quality.json` с массивом `issue` больше не печатает `a,b` через `esc()`.
14. Методика: частичный факт считается как `max(план − факт, 0)`.
15. Устаревший комментарий «чертёж .ilg не декодирован» убран.
16. КТГ: в `ktg.json` TOPO `p/pm` — план, `a/am` — факт. Раньше факт подписывался как КИО.
17. Строка заказа ТОРО: цена ДП, склады, график закупки к дате начала, явный статус «не заказано».
18. Клик по коду ЕКМТР открывает карточку снабжения: склады, график прихода, прайс, ATP.
19. PM-06 план и факт сливаются в зерно заказ×материал: закрытый заказ 1200407027 больше не висит дефицитом. Открытая потребность WK: 1 048 заказов, 3 073 млн ₽ (было 2 044 / 4 578 млн из‑за несшитых строк факта).
20. Подтянут срез МТР подрядчика (УСО) по WK: 258 заказов, 942 строки, источник `rawdata/УСО`.

## Контрольные цифры (снимок 14.09.2026, потребность пересобрана 21.09.2026)

Источник: `data/provision.json` после слияния план/факт PM-06.

| Показатель | Было (построчно) | Стало (зерно заказ×материал) |
|---|---|---|
| Открытых заказов WK | 2 044 | 1 048 |
| Потребность WK | 4 578 млн ₽ | 3 073 млн ₽ |
| Склад / закупка к сроку | 52,3% / 9,8% | 65,6% / 8,4% |
| Не покрыто ничем | — | 18,6% (573 млн ₽) |
| УСО WK | не было | 258 заказов, план 582 млн ₽, факт 381 млн ₽ |

## Сознательно не делается

- Транзитивные замены и пересчёт ATP «как будто замена на складе».
- Подстановка книги K1861 на борты №1222/1223/1224 без заводского номера.
- Дефицит по ГСМ / общему крепежу (нет витрины остатков).
- Публикация `media/kb/` на GitHub Pages (2,8 ГБ).
- Точка заказа / страховой запас — формулы в PLAN нет в коде и не выдумывается.
- Площадка склада, если в MM-M03 нет ни кода БЕ, ни узнаваемого имени — показывается склад как есть, не угадывается.

## Как открыть

Локально: `git clone -b grok` и `index.html`.  
Pages: workflow срабатывает на `grok`, `GBT` и `catalog-crm`; в артефакт входят график и чертежи LinkOne.
