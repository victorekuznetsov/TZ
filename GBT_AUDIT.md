# Аудит и улучшения ветки GBT

База: `catalog-crm` (`968c4fc`). Проверены портал WK CRM, отчётные разделы,
каталог LinkOne и 15 статических витрин `data/`.

## Найдено и исправлено

1. Переходы LinkOne `K…` не находили страницы `DK…`.
2. Не учитывались ссылки `1-K…` и варианты номеров с суффиксом `A`.
3. Глобальный поиск фактически работал только по прайсу.
4. Каталог не искал по новому артикулу, ЕКМТР, китайскому названию, ТНВЭД и узлу.
5. Номера нормализовались только по дефисам.
6. CSV допускал формулы Excel из значений `=`, `+`, `-`, `@`.
7. Таблицы скрывали строки сверх лимита без раскрытия.
8. Сортировка смешанных типов была нестабильной.
9. График делил на ноль при одном периоде или нулевом диапазоне.
10. Полнотекстовый поиск мог показать ответ предыдущего запроса.
11. Поиск LinkOne прекращался после 401 совпадения.
12. Корзина не проверяла структуру данных из `localStorage`.
13. Итог корзины не обновлялся после изменения количества.
14. Диалог детали не управлял фокусом.
15. Тема не сохранялась и принудительно была тёмной.
16. Вкладки не имели прямых URL и истории Back/Forward.
17. Фильтры каталога терялись при навигации.
18. На мобильном экране скрывался основной поиск.
19. Интерфейс ошибочно утверждал, что загрузка остатков пересчитывает обеспеченность.
20. Не обрабатывалась ошибка загрузки изображения LinkOne.
21. Интерактивные строки и дерево были недоступны с клавиатуры.
22. Заголовки таблиц не сообщали направление сортировки.

## Реализованные улучшения

1. Алиасы LinkOne `DK… ↔ K…`.
2. Нормализация префиксов позиции и суффиксов варианта.
3. Единый глобальный поиск по всем витринам.
4. Расширенный поиск каталога по 10 полям.
5. Unicode-нормализация номера детали.
6. Debounce тяжёлых поисков.
7. Сохранение состояния фильтров.
8. Кнопка сброса фильтров.
9. Понятное пустое состояние.
10. «Показать ещё» и «Показать все».
11. Клавиатурная сортировка таблиц.
12. `aria-sort` для колонок.
13. Открытие строк Enter/Space.
14. Защита CSV от formula injection.
15. Валидация корзины.
16. Живой пересчёт итога корзины.
17. Доступный счётчик корзины.
18. URL-хэши и история навигации.
19. Навигация по вкладкам стрелками, Home/End.
20. Сохранение темы и системная тема по умолчанию.
21. Мобильный глобальный поиск.
22. Доступный модальный диалог с возвратом фокуса.
23. Блокировка прокрутки фона при открытой карточке.
24. Обработка отсутствующего чертежа.
25. Клавиатурный доступ к книгам, дереву и выноскам.
26. Устойчивые графики и доступное описание SVG.
27. Поддержка `prefers-reduced-motion`.
28. Устранение гонки полнотекстового индекса.
29. Честное описание области браузерного пересчёта.
30. Автоматический аудит связей и файлов данных.

Остаётся предупреждение: в исходных книгах LinkOne 59 ссылок ведут на
отсутствующие страницы. Портал не создаёт для них ложные переходы.

## Проверка

```bash
node --check app.js
python -m compileall -q build tests
python tests/audit_data.py
node tests/test_helpers.mjs
git diff --check
```

